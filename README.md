# emanuelegian.com

Personal site — Nuxt one-pager (EN first; IT/PL later).

## Dev

```bash
npm install
npm run dev
```

## Build (GitHub Pages)

```bash
npm run generate
```

Static output: `.output/public`

On GitHub: enable **Pages → GitHub Actions**, push to `main`, then map custom domain `emanuelegian.com`.
